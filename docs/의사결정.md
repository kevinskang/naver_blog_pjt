# 의사결정 로그

> 구현 과정에서 방향 선택이 필요한 항목을 기록하고 결정 내용을 추적합니다.  
> 관련 문서: [[개선내용_20260615]] | [[Progress]] | [[VibeCoding/프로젝트/Naver_blog_pjt/설계/에러로그]]

---

## 형식

```
### DEC-NNN | [제목]
- **날짜**: YYYY-MM-DD
- **상태**: 🟡 미결 / 🟢 결정 / 🔴 보류
- **배경**: 어떤 상황에서 결정이 필요한가
- **선택지**: A안 / B안 / C안
- **결정**: 선택한 안과 이유
- **영향 범위**: 어떤 코드/기능에 영향을 주는가
```

---

## 미결 항목

_현재 미결된 항목이 없습니다._

---

## 결정 완료 항목

### DEC-001 | CAPTCHA 자동 처리 전략
- **날짜**: 2026-06-15
- **상태**: 🟢 결정
- **배경**: 현재 네이버 로그인 시 CAPTCHA가 발생하면 자동화가 중단됨. 장기적으로 완전 무인 자동화를 위해 처리 전략이 필요함.
- **선택지**:
  - **A안**: `playwright-stealth` 패키지 적용 — 브라우저 자동화 감지를 최소화하여 CAPTCHA 발생 빈도 감소
  - **B안**: 2captcha / Anti-Captcha 외부 서비스 연동 — 유료 API로 CAPTCHA 자동 풀이
  - **C안**: 현행 유지 — 세션이 유효한 동안은 CAPTCHA 없이 동작, 만료 시 수동 처리 안내
- **고려 사항**:
  - A안: 무료, 효과 불확실, 네이버 정책 변경에 취약
  - B안: 유료(월 수천 원 수준), 높은 성공률, 서비스 의존성 생김
  - C안: 추가 비용 없음, 완전 자동화 불가
- **결정**: C안 (현행 유지) 채택. 실제 CAPTCHA 빈도 대비 구현 공수 및 외부 API 의존성 리스크를 고려하여 우선 수동 개입 및 세션 체크 툴 유도 가이드를 유지하기로 함. 또한, 로그인 감지 최소화를 위해 stealth 옵션을 적용함.
- **영향 범위**: `automation/login.py`, `services/session_manager.py`

---

### DEC-002 | 멀티 계정 지원 아키텍처
- **날짜**: 2026-06-15
- **상태**: 🟢 결정
- **배경**: 여러 네이버 계정을 하나의 MCP 서버에서 관리해야 하는 요구사항
- **선택지**:
  - **A안**: Tool 파라미터에 `account_id` 추가 — 호출할 때마다 계정 지정 ← **채택**
  - **B안**: 계정별 별도 MCP 서버 인스턴스 실행
  - **C안**: `set_active_account` Tool로 전역 계정 전환
- **고려 사항**:
  - 하나의 서버에서 여러 계정 동시 사용 가능 (B안은 불가)
  - Tool 호출 시 명시적으로 계정 지정 → 실수 없음 (C안은 전역 상태 오염 위험)
  - `build_tool_metadata`에서 자동 주입 → 기존 Tool 코드 변경 최소화
- **결정**: A안 채택.
  - `_ACCOUNT_ID_PROP` 상수 정의 → `build_tool_metadata`에서 모든 Tool 스키마에 자동 주입
  - BrowserContext 풀: `_extra_contexts: Dict[str, Optional[BrowserContext]]`
  - 세션 파일: `playwright-state/auth_{account_id}.json`
  - 환경변수: `NAVER_ACCOUNT_{ID}_ID`, `NAVER_ACCOUNT_{ID}_PASSWORD`
  - 기본 계정(`account_id=""`)은 기존 동작 그대로 유지 (하위 호환)
- **영향 범위**: `config.py`, `server.py`, `services/session_manager.py`, `mcp/tools.py`

---

### DEC-003 | 글 삭제 기능 재활성화 시점
- **날짜**: 2026-06-15
- **상태**: 🟢 결정
- **배경**: `naver_blog_delete_post` Tool이 코드상 주석으로 비활성화되어 있음. Phase 1-1에서 활성화 예정이나, 삭제는 되돌릴 수 없는 작업이므로 안전장치 설계가 필요.
- **선택지**:
  - **A안**: 단순 활성화 — 확인 팝업 처리 후 즉시 삭제 ← **채택**
  - **B안**: 휴지통 이동 방식 — 네이버 블로그 관리자의 휴지통 기능 활용 (복구 가능)
  - **C안**: dry_run 파라미터 추가 — `dry_run=true` 시 삭제하지 않고 대상만 반환
- **고려 사항**:
  - 네이버 블로그 UI에 휴지통 기능이 있는지 확인 필요 → [[delete-post-research]] 참고
  - C안은 Claude가 실수로 삭제하는 것을 방지하는 데 효과적
- **결정**: A안 (단순 활성화) 채택. 멀티프레임 제어 및 다이얼로그 비동기 수락 처리 적용하여 구현 완료.
- **영향 범위**: `automation/post_actions.py`, `mcp/tools.py`
